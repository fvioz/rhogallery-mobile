<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @galleryapp.name %></h1>
    <a href="<%= url_for :action => :show, :id => @galleryapp.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @galleryapp.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @galleryapp.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="galleryapp[name]" class="fieldLabel">Name</label>
            <input type="text" id="galleryapp[name]" name="galleryapp[name]" value="<%= @galleryapp.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
