<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @buildinstall.name %></h1>
    <a href="<%= url_for :action => :show, :id => @buildinstall.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @buildinstall.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @buildinstall.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="buildinstall[name]" class="fieldLabel">Name</label>
            <input type="text" id="buildinstall[name]" name="buildinstall[name]" value="<%= @buildinstall.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
