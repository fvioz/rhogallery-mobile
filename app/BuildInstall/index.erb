<div data-role="page">

	<div data-role="header" data-position="inline">
    <h1>BuildInstalls</h1>
    <a href="<%= Rho::Application.startURI %>" class="ui-btn-left" data-icon="home" data-direction="reverse">
      Home
    </a>
    <a href="<%= url_for :action => :new %>" class="ui-btn-right" data-icon="plus">
      New
    </a>
	</div>

  <div data-role="content">
    <ul data-role="listview">
      <% @buildinstalls.each do |buildinstall| %>
      
          <li>
            <a href="<%= url_for :action => :show, :id => buildinstall.object %>">
              <%= buildinstall.name %>
            </a>
          </li>
      
      <% end %>
    </ul>
  </div>

</div>
